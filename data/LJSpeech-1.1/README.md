# Local LJSpeech directory

Place `metadata.csv` here and extracted source WAV files in `wavs/`.

The dataset itself is intentionally excluded from version control. See
[`../../docs/DATASETS.md`](../../docs/DATASETS.md) for the complete expected layout.
